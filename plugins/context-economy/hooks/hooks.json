{
  "description": "Once per session, when assistant turns >= CONTEXT_NUDGE_TURNS (default 100) AND human turns >= CONTEXT_NUDGE_MIN_USER_TURNS (default 5), nudge to apply the context-economy clear-vs-compact decision (handoff/clear/compact). Composite gate eliminates agentic fan-out false positives. Reminder only — never blocks Stop.",
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/context-reset-nudge.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
