# Fenced Marker Only

## Claim With Marker Inside Fence

This must be grounded according to the spec.

```markdown
<!-- see: references/example.md#anchor -->
```

The marker is only inside the fence, so it doesn't satisfy rule (a).
