# Example

## Conventions

<!-- cartographer:managed:start conventions -->
Every module registers through the `exporter registry`, per [CONVENTIONS.md](CONVENTIONS.md).

The `module manifest` is regenerated on every build.
<!-- cartographer:managed:end conventions -->

Illustrative example, shown for documentation only (fenced, so ignored
by the marker scan and the link/command gates):

```markdown
Every module registers through the `exporter registry`, per [CONVENTIONS.md](CONVENTIONS.md).
```
