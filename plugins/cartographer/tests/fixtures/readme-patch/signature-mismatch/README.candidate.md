# Example

## Usage

<!-- cartographer:managed:start usage -->
The store is opened with `openSession(options)` — see [lib/session-store.js](lib/session-store.js).
<!-- cartographer:managed:end usage -->

Illustrative example, shown for documentation only (fenced, so ignored
by the marker scan and the link/command gates):

```markdown
The store is opened with `openSession(options)` — see [lib/session-store.js](lib/session-store.js).
```
